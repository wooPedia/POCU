#include "ERounding.h"

namespace assignment3
{
	//// value에 대해 n번째 자리에서 반올림합니다.
	//double roundHalfUp(double value, eRounding n)
	//{
	//	return std::round(value * static_cast<int>(n)) / static_cast<int>(n);
	//}
}